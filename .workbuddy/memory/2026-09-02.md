# 2026-09-02 工作日志

## 达冠报告两处缺陷修复（落地，已端到端验证）
用户指令："直接改，不能限字数，而且报告内容不能固定"。

### 改动文件：engine/enterprise_report.py
1. **去掉固定字数截断（江苏2 修复）**
   - `_build_summary`(原 657 行)：`key_points.append(...{_brief(first, limit=110)})` → 直接拼完整首段 `{first}`，不再限字数。
   - `_brief`(35 行) 兜底由硬切 `cut.rstrip(...)+"。"` 改为 `return t`（无句末标点时保留完整内容，不丢确定性数字串）。影响 discovery_overview 的 one_line(706) 等同理。
2. **报告输出中文化归一（英文修复）**
   - 新增 `_ZH_REPLACEMENTS` + `_zh_normalize(text)` + `_zh_normalize_obj(o)`（递归对报告字典全部字符串值归一）。
   - 映射：detect→verify→diagnose→report→检测→核验→诊断→报告；detect/verify/diagnose/rectify/report 单动词；vs→与；BOM→物料清单；ETS→电子税务局。
   - 关键坑：`\b` 在 Python 把中文当 `\w`，导致 `BOM版本`/`省ETS` 不匹配；改用 ASCII 字母前瞻/后顾 `(?<![A-Za-z])...\b`(?![A-Za-z])` 边界才生效。
   - `build_enterprise_readable_report`(1267) 返回值整体包 `_zh_normalize_obj({...})`。

### 验证（真实 company_2_full.json）
- 重点3 关键句现含完整"江苏2家989,416元(6%)"，不再截断。
- 整份可读报告英文残留命中 = 0（BOM 12→0、vs 5→0、verify/methodology→0、ETS→0）。
- `py_compile` OK，`build_enterprise_readable_report` 实跑无异常（24 字段）。

### 备注
- "省ETS（社保费）"为银行流水原始对手方名称（ETS=电子税务局），仅存在于 raw file_results，不在可读报告内（可读报告 ETS 计数 0）；若用户在原始数据表视图看到，可再在 domain_analysis.py:5616 对手方分类处做源端中文化。
- 修复只在报告生成层；引擎 finding 文本仍含英文缩写（被归一化前），如需源头纯中文可后续改 domain_analysis/verified_rule_engine。

## 源端中文化（用户补充指令："改"）
用户确认要把原始数据表的英文对手方也变中文。

### 改动文件：engine/domain_analysis.py
- 新增模块级 `_normalize_counterparty_name(name)`：`re.sub(r"(?<![A-Za-z])ETS(?![A-Za-z])","电子税务局",name)`，仅匹配独立英文词 ETS，避免误伤含 ET 中文词。
- 在全部银行流水对手方抽取点调用：共 6 处——`_extract_material_intel` 内 5460/5513/5553(收款/汇总/纠错)、5621(付款分类 5616 区域)、以及 2290/3296/3513/3795(银行收款汇总/股东款/供应商付款时间线/运输公司检测) 等独立循环。
- 同步分类关键词：5487 `["社保","医保","税","国库","ETS","电子税务局"]`、5510/5608 summary 命中补 `电子税务局`、5616 `['国家金库','税务局','电子税务局','社保','国库','税','财政']`（移除原 ETS/省ETS，因 cp 已归一）。

### 改动文件：static/js/bank-vat.js（原始数据表视图，非破坏性显示归一）
- 新增全局 `zhBankText(s)`：`String(s).replace(/\bETS\b/g,'电子税务局')`，仅展示层，不改存储（审计追溯完整）。
- 应用到 3 处渲染：122 行 `counterparty_name`、127 行 `summary`、1101 行 `bank.counterparty`（发票-银行匹配对方）。

### 验证
- Python 正则 / Node 均确认：`省ETS（社保费）`→`省电子税务局（社保费）`、`社保广东省ETS扣税缴费`→`社保广东省电子税务局扣税缴费`、中文名不受影响。
- 重建达冠可读报告：英文残留 0、重点3 江苏完整（回归通过）。
- 生效前提：后端需重启（8001，start8001.bat）+ 前端刷新静态文件。

### 结论
- 报告正文（enterprise_report 递归归一）+ 情报/分类（domain_analysis 源端归一）+ 原始流水表（bank-vat 显示归一）三处联动，达冠报告及原始数据视图英文缩写（BOM/vs/ETS/detect→verify）已全部清除。
- 唯一未动：DB 中已落库的原始流水行（刻意不改，保审计可追溯）；用户所见已全中文。

## 死代码清理 + 报告导出PDF + 全部推送（用户指令："清除全部死代码，并全部推送" → "设置报告导出PDF按钮" → "都做，按顺序进行"）

### 1. 删除废弃文件（11 个）
- 未跟踪 scratch：`_xref.py`、`scripts/_costtest.py/_elemtest.py/_geotest.py/_invfields.py/_probe_riskbird*.py/_trtest.py`。
- 未被任何 live 模块 import 的 `engine/riskbird_client.py`（仅被已删探针引用）。
- 已跟踪的遗留迁移脚本 `static/js/rewrite_methodology.py`、`static/js/update_analyze_page.py`（审计 P2 标记，指向不存在路径）。

### 2. 删除 tax-pipeline-pages.js 被遮蔽的旧方法论渲染器（~1558 行）
- 定位方法：枚举死代码区间顶层定义 + 逐个核验"死区间外出现次数"（函数名全为 0，generic 局部变量名误报靠人工排除）。
- 删除两段：1531–1731（`renderAnalyzePage` 及其 helper `_methodologyResult*` 簇）与 6164–7519（`METHODOLOGY_PAGE_SECTIONS` + `_loadMethodology*` + 旧 `renderMethodologyPage` + `renderUnifiedDomainPanelV4` + `_renderMethodologyOverview` 等）。
- 共享渲染器 `renderTaxRiskRules`(tax-risk-rules.js)、`renderFileParsingPage`(63 行) 不在死区间内，保留。
- 删除前确认：`.method-mount` CSS 作用域仅在旧 renderMethodologyPage 内，无 live 容器，删除无视觉回归（live 表 `rr-table method-chain-table` 的样式来自 `rr-table`，非 `.method-mount .method-chain-table`）。
- 用带边界断言的 Python 脚本按行号精确删除 + 回写，`node --check` 通过。

### 3. core.js 精简 methodology 路由
- 旧 `else if (typeof renderMethodologyPage === 'function')` 回退检测分支在旧渲染器删除后已不可能命中 → 简化为 if/else（v3 加载标记 + 函数存在 → 渲染；否则报错）。

### 4. enterprise_report.py 移除失效 `_brief`
- 用户"不能限字数"：删除整个 `_brief(text, limit=180)`（仅剩 746 一处调用，且仍截断），调用点改为 `_norm_text(first_para.get("text",""))`，消除失效 `limit` 形参。

### 5. 报告「导出PDF」按钮（tax-doc-analysis.js）
- 工具栏新增按钮 `tda-export-pdf-btn` + 验收门禁同步控制（`[exportBtn, pdfBtn].forEach`）。
- 新增 `exportTaxDocReportPdf()`：复用交付接口 `/api/compliance/rounds/{id}/deliver`（format html，自包含内联样式、草稿带水印）→ 新窗口 `document.write` → `window.print()`（浏览器"另存为 PDF"），离线零依赖。

### 6. 测试 test_business_api_alignment.py 三处修复
- 删除死代码导致 3 个测试方法断言失效，改为指向现行 `methodology-v3.js`：
  - `test_dashboard_...`：方法论块(571–691)重写为断言 v3（`__METHODOLOGY_V3_LOADED__`、`window.renderMethodologyPage`、m3-* section id、7 个 fetch asset、`242项真实能力账本`、安全边界 assertNotIn），保留 main.py/risk_rules 断言。
  - `test_module_pages_use_compact_outer_gutters`：循环去掉 tax-pipeline-pages.js，新增 `methodology-v3.js` 的 `max-width:1480px;margin:0 auto`；删除 `.method-mount` chain_selector 断言。
  - `test_tables_and_support_pages_use_executive_layouts`：删除 `.method-chain-table{min-width:840px` 断言（count==3 与 colgroup 仍过）。
- 验证：3 个方法全过；全文件 11 测试 9 过、2 失败均为预存在 fastapi 缺失（子进程 import main.py），与本次改动无关。

### 7. 提交推送
- `git add -A` + commit `d63773d8`「死代码清理 + 报告导出PDF + 报告修复」+ `git push origin main` 成功。
- 远程 `git ls-remote origin main` = `d63773d8` = 本地 HEAD，无分叉。本地 `refs/remotes/origin/main` 显示 `ahead 141` 为沙箱下跟踪引用未持久化的显示问题，不影响实际远程状态。

### 关键经验（可复用）
- 删除 JS 死代码的安全流程：①枚举死区间顶层 `function/var/window.X` 定义 → ②逐个核验"区间外出现次数"（0=死，>0 需人工排除 generic 局部变量名与注释误报）→ ③带边界断言的脚本按行号精确删 → ④`node --check` → ⑤同步 grep 全局引用改测试。切勿手改大块，避免 off-by-one。
