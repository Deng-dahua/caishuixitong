# 2026-09-01 工作日志

## 达冠报告两处缺陷根因诊断（company_id=2 中山市达冠纺织）
用户质询：①报告为何仍有英文；②"重点3"句末"江苏2"信息不全。

### Q1 英文来源（已定位）
- 英文非报告框架问题，而是**规则引擎 finding 文本里直接写死的英文缩写/术语**，被 enterprise_report.py 原样拼入报告：
  - `省ETS`（电子税务局 Electronic Tax System）：`engine/domain_analysis.py:5616` 对手方分类 `['...','ETS','...','省ETS']` → 银行流水对手方显示"省ETS（社保费）"。
  - `BOM`（物料清单）：供应链/存货勾稽 suggestion（如"由生产工单连接BOM版本…"），源自 domain_analysis/main.py BOM 分析，可读报告出现 12 次。
  - `vs`（versus）：比对句"X vs Y"，5 次。
  - `detect→verify→diagnose→…`：四步税务合规分析法英文动词（AGI/orchestrator 方法论描述）。
  - 流水线诊断日志 `finding/Provenance/Benford/EMA/DB/Phase3`：位于 pipeline_log/AGI 日志，若渲染"诊断过程"会露出。
- 结论：enterprise_report.py 对上游 finding 的 type/detail/suggestion 不做中文化归一，缩写被信任直出。修复点二选一：①报告层 `_problem_paragraphs` 后处理替换缩写；②源头 domain_analysis/verified_rule_engine 写中文。

### Q2 "江苏2"截断（已定位·确定性）
- 根因 `engine/enterprise_report.py:657`：`key_points.append(f"重点{p['seq']}{grade_tag}：{title}。{_brief(first, limit=110)}")`
- `_brief(text, limit=180)`（行 35）：前 110 字内无句末标点（。；！？）→ 走兜底 `cut.rstrip("，、；, ")+"。"` 硬切。
- 重点3 首段为单长句，110 字处正好切在"…湖北1家…(7%)、江苏2"后，第 5 省江苏被截成"江苏2"（丢失"家143,xxx元(7%)。")。
- 引擎 verified_rule_engine.py:1343-1348 产出**完整**串；正文 narrative_paragraphs（行 489）也完整保存——仅 110 字重点摘要被截，数据未丢。
- 修复：将 `limit=110` 调大（≥300）或重点摘要对确定性数字串不做截断。

### 待办
- 用户未要求修复，仅诊断。修复需明确指令。
