# 2026-08-26 工作日志

## 数电票下"开票 vs 自报"差异讨论与路线图修正
- 用户指出：数电票 + 申报自动预填 + 一窗式比对已锁死"已开票未申报"差异，财务不会手动砍已开票收入。
- WebSearch 核实官方口径：税务数字账户预填销项/进项；有开票零申报被拦截；票表比对失败无法提交。确认用户正确。
- 我更正了前一条把"缺口B直接稽查命中"的过时说法，并明确差异迁移到：未开票收入栏、两税收入差、私户资金流、虚开。
- 重写 docs/capability_boundary_roadmap.md：云帐房从 P0 降级 P1（开票比对已冗余），新增"资金流比对"为 P0 最高优先级 + "关键认知修正/待补能力（第四阶）"章节。
- 同步更新 MEMORY.md 长期记忆，沉淀该修正。
- 未改动代码（纯文档/记忆修正）。待用户拍板接资金流数据后再写比对规则。

## 实现第四阶 P0：银行流水（资金流）比对
- 用户拍板"动手"：数电票下票表差异已锁死、银行流水是稽查必收资料，据此做资金流比对。
- 新建 engine/bank_flow.py：run_bank_flow_compare(bank_txs, sal_invs, reported_income, invoice_total, cross_enterprise) ——
  量化 ①流水收款 vs 申报收入(增值税申报表销售额/开票额) 的未开票/账外敞口 ②私户/个人收款 ③第三方平台收款 ④对公收款与开票客户不匹配 ⑤结合跨企业图谱的资金回流闭环；输出 metrics/signals/verdict，结论均标"待证线索"。
- 接入主链路 engine/pipeline.py（照 external_verifier 模式，⑤块 try/except 包裹，写入 comprehensive["bank_flow"]；申报收入从 tax_declarations 的 sales_amount 稳健提取，兼容 declaration/rows 结构）。
- 报告章节 engine/enterprise_report.py 新增 _build_bank_flow_report（第十三章）；前端 static/js/tax-doc-analysis.js 渲染 "十三、银行流水（资金流）比对"。
- 验证：unit test（scripts/test_bank_flow.py 断言全过）+ 语法检查 + 端到端（scripts/e2e_bank_flow.py 走真实 upload/analyze API，comprehensive.bank_flow 与报告章节均生成，私户识别=65万）。
- 修复：bank_flow 模块内一处 noop 叠加 bug（personal_receipt 翻倍）；样本CSV改用 贷方金额/借方金额 列（借贷标志+交易金额 在 pipeline 标准化下不会拆分 credit/debit，会被整行跳过）。
- 提交 0ad40587。服务器已重启（8001，PID 16460）。
